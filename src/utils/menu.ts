import React,{ ReactNode } from 'react';
import { IRoute } from 'umi-types';
import {
  Menu
} from 'antd'

const { SubMenu } = Menu;

export function initialMenuDOM(routes: IRoute ):ReactNode {
  let menuDOM:Array<ReactNode> = [];

  return menuDOM;
}
